import { defineConfig } from 'vite';

export default defineConfig({
    // Base public path when serving or building the project
    base: '/',
    // Add any other Vite configuration settings if needed
});
